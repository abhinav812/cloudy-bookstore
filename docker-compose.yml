version: '3.8'

services:
  bookstore-app:
    image: "${IMAGE_NAME}"
    build:
      context: .
      dockerfile: ./docker/app/Dockerfile
    env_file:
      - ./docker/app/.env
    ports:
      - "8080:8080"